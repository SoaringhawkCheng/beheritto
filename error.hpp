#include <exception>
using namespace std;

class CustomException:public expection{
    explicit LexicalException(const string &errmsg):exception(errmsg){}
};

class LexicalError:public CustomExpection{
    explicit LexicalError(const string &errmsg):exception(errmsg){}
};

class LexicalError:public CustomExpection{
    explicit LexicalError(const string &errmsg):exception(errmsg){}
};

class LexicalError:public CustomExpection{
    explicit LexicalError(const string &errmsg):exception(errmsg){}
};

class LexicalError:public CustomExpection{
    explicit LexicalError(const string &errmsg):exception(errmsg){}
};

class LexicalError:public CustomExpection{
    explicit LexicalError(const string &errmsg):exception(errmsg){}
};

